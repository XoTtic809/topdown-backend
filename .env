# ─── Server ───────────────────────────────────────────────────
# Railway sets PORT automatically — leave blank in Railway Variables
PORT=3001

# ─── PostgreSQL ────────────────────────────────────────────────
# Railway provides this automatically when you add a Postgres service.
# Copy the DATABASE_URL from your Railway Postgres → Variables tab.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# ─── JWT ──────────────────────────────────────────────────────
# Generate a strong secret: run this in your terminal:
#   openssl rand -base64 32
# Then paste the result here. Treat it like a password — never share it.
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_STRING_AT_LEAST_32_CHARS

# ─── CORS ──────────────────────────────────────────────────────
# Set to your frontend URL in production.
# Example: https://yourgame.github.io
# For local dev you can use * to allow all origins.
ALLOWED_ORIGIN=*

# ─── Environment ──────────────────────────────────────────────
# Set to "production" on Railway — this enables SSL for Postgres.
NODE_ENV=development
